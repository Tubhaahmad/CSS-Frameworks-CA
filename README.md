# CSS Frameworsk CA

## 🚀 Project Setup
This project was built using Vite, Bootstrap (with Sass), and Bootstrap Icons.
It includes an Authentication page, Feed page, and Profile page, styled with SCSS using variables, mixins, and responsive design.

## 📦 How to Run the Project
After cloning this repository, open your terminal and run:

```bash
cd CSS-frameworks-CA
```

```bash
npm install
```

```bash
npm run dev
```
Then open the local server URL shown in the terminal (usually http://localhost:5173/).

## 🏗️ Building for Production

To generate a production build, run:

```bash
npm run build
```

Then preview the build using:
```bash
npm run preview
```

## 🛠️ Built With

Vite

Bootstrap 5 (Sass)

Bootstrap Icons

Sass

